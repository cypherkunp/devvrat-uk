# Deploy on Vercel at devvrat.uk

The Link Hub is deployed on Vercel with the custom domain `devvrat.uk` mapped to it. `devvrat.cc` remains a separate property and is only reached as the Central Hub Link destination.
